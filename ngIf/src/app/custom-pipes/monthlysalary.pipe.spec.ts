import { MonthlysalaryPipe } from './monthlysalary.pipe';

describe('MonthlysalaryPipe', () => {
  it('create an instance', () => {
    const pipe = new MonthlysalaryPipe();
    expect(pipe).toBeTruthy();
  });
});
